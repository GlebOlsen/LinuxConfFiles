# Placeholder. On nix-laptop run:
#   cp /etc/nixos/hardware-configuration.nix ./hardware.nix
# Then commit. Replace this file entirely.
{ ... }:
{
  # Intentionally empty until real hardware-configuration.nix committed.
}
